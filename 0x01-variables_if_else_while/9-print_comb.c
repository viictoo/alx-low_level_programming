#include <stdio.h>

/**
 * Description:main-A program to print comma separated numbers
 *
 * Return: 0(success)
 *
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
	putchar(i);
	if ( i == 9)
		continue;
	putchar(',');
	putchar(' ');
	}
	putchar('\n');
	return (0);
}
