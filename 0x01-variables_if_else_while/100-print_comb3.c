/**
 * Description:main-A program to print comma separated numbers
 *
 * Return: 0(success)
 *
 */

int main(void)

{
	int a;
	int y;
	int b;
	int x;


	for (a = 0; a < 10; a++)
	{
	for (b = 0; b < 10; b++)
	{
		{
		y = a;
		x = b;

		if (ab == xy)
	{
		if (xy < ab)
			break;
	}
		putchar(a);
		putchar(b);
		putchar(',');
		putchar(' ');
	}
	}
	}
	putchar('\n');
	return (0);
}
